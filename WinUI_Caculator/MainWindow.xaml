<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="WinUI_Calculator.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUI_Calculator"
    xmlns:views="using:WinUI_Calculator.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="WinUI Calculator">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 左側模式選擇 -->
        <ListBox x:Name="ModeList"
                 Grid.Column="0"
                 SelectionChanged="ModeList_SelectionChanged"
                 FontSize="20"
                 HorizontalContentAlignment="Stretch">
            <ListBoxItem Content="加減乘除"/>
            <ListBoxItem Content="進制轉換"/>
            <ListBoxItem Content="溫度轉換"/>
        </ListBox>

        <!-- 右側 UserControl 顯示區 -->
        <ContentControl x:Name="ModeContent"
                        Grid.Column="1"
                        Margin="10"/>
    </Grid>
</Window>
