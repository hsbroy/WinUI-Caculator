<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="WinUI_Calculator.Views.BaseConversionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUI_Caculator.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="120"/>
            <RowDefinition Height="60"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- 顯示區 -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="{Binding DisplayText}"
                   FontSize="48"
                   HorizontalAlignment="Right"/>
            <TextBlock Text="{Binding MessageText}"
                   Foreground="Red"
                   FontSize="16"
                   HorizontalAlignment="Right"/>
        </StackPanel>

        <!-- 進制選擇 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Spacing="15">
            <Button Content="BIN" Command="{Binding BaseConversionCommand}" CommandParameter="BIN"/>
            <Button Content="OCT" Command="{Binding BaseConversionCommand}" CommandParameter="OCT"/>
            <Button Content="DEC" Command="{Binding BaseConversionCommand}" CommandParameter="DEC"/>
            <Button Content="HEX" Command="{Binding BaseConversionCommand}" CommandParameter="HEX"/>
        </StackPanel>

        <!-- 數字鍵 -->
        <Grid Grid.Row="2" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Button Grid.Row="0" Grid.Column="0" Content="0" Command="{Binding NumberCommand}" CommandParameter="0"/>
            <Button Grid.Row="0" Grid.Column="1" Content="1" Command="{Binding NumberCommand}" CommandParameter="1"/>
            <Button Grid.Row="0" Grid.Column="2" Content="2" Command="{Binding NumberCommand}" CommandParameter="2"/>
            <Button Grid.Row="0" Grid.Column="3" Content="3" Command="{Binding NumberCommand}" CommandParameter="3"/>

            <Button Grid.Row="1" Grid.Column="0" Content="4" Command="{Binding NumberCommand}" CommandParameter="4"/>
            <Button Grid.Row="1" Grid.Column="1" Content="5" Command="{Binding NumberCommand}" CommandParameter="5"/>
            <Button Grid.Row="1" Grid.Column="2" Content="6" Command="{Binding NumberCommand}" CommandParameter="6"/>
            <Button Grid.Row="1" Grid.Column="3" Content="7" Command="{Binding NumberCommand}" CommandParameter="7"/>

            <Button Grid.Row="2" Grid.Column="0" Content="8" Command="{Binding NumberCommand}" CommandParameter="8"/>
            <Button Grid.Row="2" Grid.Column="1" Content="9" Command="{Binding NumberCommand}" CommandParameter="9"/>

            <Button Grid.Row="3" Grid.Column="3" Content="C" Command="{Binding ClearCommand}"/>
        </Grid>
    </Grid>
</UserControl>
